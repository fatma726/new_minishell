# --- Add these targets to your Makefile (snippet) ---
leaks:
	@mkdir -p tests/out tests/valgrind_logs/batch1
	@bash tests/run_batch.sh tests/manifest.csv

eval:
	@bash tests/run_batch.sh tests/manifest.csv
# -----------------------------------------------
